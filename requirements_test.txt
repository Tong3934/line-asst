# requirements_test.txt
# ─────────────────────────────────────────────────────────────────
# Test-only dependencies for the LINE Insurance Claim Bot test suite.
# Install with:  pip install -r requirements_test.txt
# ─────────────────────────────────────────────────────────────────

# ── Core test framework ──────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-timeout>=2.3.0
pytest-cov>=5.0.0

# ── HTTP test client (FastAPI) ───────────────────────────────────
httpx>=0.27.0          # required by FastAPI TestClient
# Note: fastapi[all] is already in requirements.txt

# ── Stubs / mocking ─────────────────────────────────────────────
# unittest.mock is stdlib; no extra install needed.

# ── Pillow (for dummy image generation in tests) ─────────────────
Pillow>=10.0.0         # already in main requirements.txt, listed for clarity

# ── Optional: nicer output ───────────────────────────────────────
pytest-sugar>=1.0.0    # progress bar

# ── HTML test reports ────────────────────────────────────────────
pytest-html>=4.0.0     # self-contained HTML report (test-report/SIT-yyyymmdd-nn/)
