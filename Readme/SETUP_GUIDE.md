# 📖 คู่มือการติดตั้งและใช้งาน LINE Insurance Claim Bot

## 📥 ขั้นตอนที่ 1: ติดตั้ง Python และ Dependencies

### 1.1 ตรวจสอบ Python Version

ต้องใช้ Python 3.8 ขึ้นไป:

```bash
python --version
# หรือ
python3 --version
```

### 1.2 สร้าง Virtual Environment (แนะนำ)

```bash
# สร้าง virtual environment
python -m venv venv

# เปิดใช้งาน (macOS/Linux)
source venv/bin/activate

# เปิดใช้งาน (Windows)
venv\Scripts\activate
```

### 1.3 ติดตั้ง Dependencies

```bash
pip install -r requirements.txt
```

## 🔑 ขั้นตอนที่ 2: ขอ API Keys

### 2.1 LINE Messaging API

1. **ไปที่ LINE Developers Console**
   - เปิด [https://developers.line.biz/console/](https://developers.line.biz/console/)
   - Login ด้วย LINE Account

2. **สร้าง Provider**
   - คลิก "Create" → "Create a new provider"
   - ใส่ชื่อ Provider (เช่น "Insurance Bot Provider")

3. **สร้าง Channel**
   - เลือก Provider ที่สร้าง
   - คลิก "Create a new channel"
   - เลือก "Messaging API"
   - กรอกข้อมูล:
     - Channel name: "Insurance Claim Bot"
     - Channel description: "วิเคราะห์การเคลมประกันรถยนต์ด้วย AI"
     - Category: "Finance"
     - Subcategory: "Insurance"
   - ยอมรับเงื่อนไข → คลิก "Create"

4. **ดึง Channel Access Token**
   - เข้าไปที่ Channel ที่สร้าง
   - ไปที่แท็บ "Messaging API"
   - เลื่อนลงหา "Channel access token (long-lived)"
   - คลิก "Issue" → คัดลอก Token

5. **ดึง Channel Secret**
   - ไปที่แท็บ "Basic settings"
   - เลื่อนลงหา "Channel secret"
   - คลิก "Show" → คัดลอก Secret

### 2.2 Google Gemini API

1. **ไปที่ Google AI Studio**
   - เปิด [https://makersuite.google.com/app/apikey](https://makersuite.google.com/app/apikey)
   - Login ด้วย Google Account

2. **สร้าง API Key**
   - คลิก "Create API Key"
   - เลือก Google Cloud Project (หรือสร้างใหม่)
   - คลิก "Create API key in existing project"
   - คัดลอก API Key

3. **เปิดใช้งาน API** (ถ้ายังไม่เปิด)
   - ไปที่ [Google Cloud Console](https://console.cloud.google.com/)
   - เปิดใช้งาน "Generative Language API"

## ⚙️ ขั้นตอนที่ 3: ตั้งค่า Environment Variables

1. **สร้างไฟล์ .env**

```bash
cp .env.example .env
```

2. **แก้ไขไฟล์ .env**

เปิดไฟล์ `.env` ด้วย text editor และใส่ค่าที่ได้จากขั้นตอนที่ 2:

```env
LINE_CHANNEL_ACCESS_TOKEN=YOUR_ACTUAL_CHANNEL_ACCESS_TOKEN
LINE_CHANNEL_SECRET=YOUR_ACTUAL_CHANNEL_SECRET
GEMINI_API_KEY=YOUR_ACTUAL_GEMINI_API_KEY
PORT=8000
```

⚠️ **คำเตือน**: อย่า commit ไฟล์ `.env` ขึ้น Git!

## 🚀 ขั้นตอนที่ 4: รันโปรแกรม

### 4.1 รันในโหมด Development

```bash
python main.py
```

เซิร์ฟเวอร์จะรันที่: `http://localhost:8000`

### 4.2 ทดสอบว่ารันสำเร็จ

เปิด browser ไปที่:
- `http://localhost:8000` → ควรเห็นข้อความ "LINE Insurance Claim Bot API"
- `http://localhost:8000/health` → ควรเห็น status "healthy"

## 🌐 ขั้นตอนที่ 5: ตั้งค่า Webhook (สำหรับทดสอบ)

เนื่องจาก LINE ต้องการ HTTPS URL, เราจะใช้ ngrok สำหรับทดสอบ

### 5.1 ติดตั้ง ngrok

**macOS (Homebrew):**
```bash
brew install ngrok
```

**Windows/Linux:**
- ดาวน์โหลดจาก [https://ngrok.com/download](https://ngrok.com/download)
- แตกไฟล์และติดตั้ง

### 5.2 รัน ngrok

เปิด terminal ใหม่และรัน:

```bash
ngrok http 8000
```

คุณจะได้ URL แบบนี้:
```
Forwarding    https://xxxx-xx-xxx-xxx-xx.ngrok-free.app -> http://localhost:8000
```

คัดลอก URL `https://xxxx-xx-xxx-xxx-xx.ngrok-free.app`

### 5.3 ตั้งค่า Webhook บน LINE

1. **กลับไปที่ LINE Developers Console**
   - เข้าไปที่ Channel ของคุณ
   - ไปที่แท็บ "Messaging API"

2. **ตั้งค่า Webhook URL**
   - เลื่อนลงหา "Webhook settings"
   - คลิก "Edit"
   - ใส่ URL: `https://xxxx-xx-xxx-xxx-xx.ngrok-free.app/webhook`
   - คลิก "Update"

3. **เปิดใช้งาน Webhook**
   - สลับสวิตช์ "Use webhook" เป็น ON

4. **ตรวจสอบ Webhook**
   - คลิกปุ่ม "Verify" เพื่อทดสอบว่า webhook ทำงานหรือไม่
   - ถ้าสำเร็จจะขึ้น "Success"

5. **ปิด Auto-reply (ถ้าต้องการ)**
   - เลื่อนลงหา "Auto-reply messages"
   - คลิก "Edit"
   - ปิด "Auto-reply messages"

## 📱 ขั้นตอนที่ 6: ทดสอบบนมือถือ

### 6.1 เพิ่มบอทเป็นเพื่อน

1. ไปที่ LINE Developers Console → Channel → "Messaging API"
2. เลื่อนลงหา "QR code"
3. สแกน QR Code ด้วยมือถือ → เพิ่มเป็นเพื่อน

### 6.2 ทดสอบการทำงาน

**Test Case 1: เริ่มต้น**
- ส่งข้อความ: `เช็คสิทธิ์เคลมด่วน`
- บอทจะตอบกลับด้วย Flex Message ขอข้อมูล

**Test Case 2: กรอกข้อมูล (ประกันชั้น 1)**
- ส่งข้อความ: `นายสมชาย เข็มกลัด, 1กข1234`
- บอทจะแสดงข้อมูลกรมธรรม์และขอรูปภาพ

**Test Case 3: ส่งรูปภาพ**
- ถ่ายรูปรถที่มีความเสียหาย (หรือใช้รูปตัวอย่าง)
- ส่งรูปภาพ
- รอ 10-30 วินาที
- บอทจะวิเคราะห์และส่งผลกลับมา

**Test Case 4: กรอกข้อมูล (ประกันชั้น 2+)**
- ส่งข้อความ: `เช็คสิทธิ์เคลมด่วน`
- ส่งข้อความ: `นางสมหญิง ใจดี, 2กท5678`
- ส่งรูปภาพ
- ดูผลการวิเคราะห์ที่แตกต่างกัน (ต้องมีคู่กรณี)

**Test Case 5: กรอกข้อมูล (รถ SUV ชั้น 1)**
- ส่งข้อความ: `เช็คสิทธิ์เคลมด่วน`
- ส่งข้อความ: `นายวิชัย รักษ์ดี, 3กก9999`
- ส่งรูปภาพ
- ดูผลการวิเคราะห์สำหรับรถ SUV (Excess สูงกว่า)

## 🐛 แก้ปัญหา

### ปัญหา: บอทไม่ตอบ

**สาเหตุและวิธีแก้:**

1. **Webhook URL ไม่ถูกต้อง**
   - ตรวจสอบว่า ngrok ยังรันอยู่
   - ตรวจสอบว่า URL ใน LINE Console ถูกต้อง
   - URL ต้องลงท้ายด้วย `/webhook`

2. **Channel Access Token ผิด**
   - ตรวจสอบไฟล์ `.env`
   - ดึง Token ใหม่จาก LINE Console

3. **ดู Logs**
   ```bash
   # Terminal ที่รัน python main.py จะแสดง error
   ```

### ปัญหา: Gemini AI ไม่ทำงาน

**สาเหตุและวิธีแก้:**

1. **API Key ไม่ถูกต้อง**
   - ตรวจสอบไฟล์ `.env`
   - สร้าง API Key ใหม่จาก Google AI Studio

2. **ยังไม่เปิดใช้งาน API**
   - เปิด Generative Language API ใน Google Cloud Console

3. **Quota เกิน**
   - ตรวจสอบ usage ใน Google Cloud Console
   - รอให้ quota reset หรืออัพเกรด

### ปัญหา: รูปภาพโหลดไม่ได้

**สาเหตุและวิธีแก้:**

1. **Channel Access Token ผิด**
   - ตรวจสอบ Token ในไฟล์ `.env`

2. **Network ปัญหา**
   - ตรวจสอบ internet connection
   - ลองส่งรูปใหม่

## 🎯 ขั้นตอนถัดไป

### การพัฒนาเพิ่มเติม

1. **เพิ่มข้อมูล Mock สำหรับทดสอบ**
   - เปิดไฟล์ `mock_data.py`
   - เพิ่มข้อมูลใน Dictionary `MOCK_POLICIES`
   - รูปแบบ key: `"ชื่อ-นามสกุล_ทะเบียนรถ"`

2. **เชื่อมต่อ Database จริง**
   - ใช้ PostgreSQL หรือ MongoDB
   - แทนที่ `get_policy_info()` ใน `mock_data.py` ด้วย database query

2. **เพิ่ม Authentication**
   - ใช้ LINE Login API
   - เชื่อมโยงกับระบบ CRM

3. **Rich Menu**
   - สร้าง Rich Menu สำหรับ shortcut
   - ปุ่มเช็คสิทธิ์, ติดตามสถานะ, ติดต่อเจ้าหน้าที่

4. **Deploy Production**
   - ใช้ Cloud Platform (AWS, GCP, Azure)
   - ตั้งค่า HTTPS จริง
   - ใช้ Load Balancer และ Auto Scaling

### Deploy บน Cloud

**Render.com (ฟรี):**
1. สร้างบัญชี [Render](https://render.com/)
2. Connect GitHub Repository
3. เลือก "Web Service"
4. ตั้งค่า Environment Variables
5. Deploy!

**Railway.app (ฟรี):**
1. สร้างบัญชี [Railway](https://railway.app/)
2. New Project → Deploy from GitHub
3. ตั้งค่า Environment Variables
4. Deploy!

## 📞 ติดต่อและสนับสนุน

หากมีปัญหาหรือข้อสงสัย:
- ดู logs ใน terminal
- อ่าน error message อย่างละเอียด
- ตรวจสอบ environment variables
- ลองรันใหม่

---

🎉 **ขอให้สนุกกับการพัฒนา!**
