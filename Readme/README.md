# 🚗 LINE Insurance Claim Bot

บอทวิเคราะห์การเคลมประกันรถยนต์อัจฉริยะด้วย AI (LINE Messaging API + Google Gemini)

## ✨ ฟีเจอร์หลัก

- 🔍 **ตรวจสอบสิทธิ์เคลม**: ตรวจสอบข้อมูลกรมธรรม์จากชื่อและทะเบียนรถ
- 📸 **วิเคราะห์ความเสียหาย**: ใช้ Google Gemini AI วิเคราะห์รูปภาพความเสียหายอัตโนมัติ
- 💰 **ประเมินค่าใช้จ่าย**: คำนวณค่าซ่อมและค่าเสียหายส่วนแรก (Excess) เบื้องต้น
- 📋 **แนะนำขั้นตอน**: ให้คำแนะนำการดำเนินการเคลมแบบละเอียด
- 🎨 **Flex Message**: UI สวยงามและใช้งานง่ายบน LINE

## 🛠️ Tech Stack

- **FastAPI**: Web framework สำหรับสร้าง API
- **LINE Bot SDK**: เชื่อมต่อกับ LINE Messaging API
- **Google Gemini AI**: วิเคราะห์รูปภาพด้วย AI แบบ Multimodal
- **Python-dotenv**: จัดการ environment variables
- **Uvicorn**: ASGI server สำหรับรัน FastAPI

## 📋 ข้อกำหนดระบบ

- Python 3.8 ขึ้นไป
- LINE Messaging API Channel (Channel Access Token + Channel Secret)
- Google Gemini API Key

## 🚀 การติดตั้งและใช้งาน

### วิธีที่ 1: ใช้ GitHub Codespaces (แนะนำสำหรับมือใหม่) ⭐

วิธีที่ง่ายที่สุด! ไม่ต้องติดตั้งอะไรเลย ได้ Public URL อัตโนมัติ

1. **สร้าง Codespace**
   - คลิกปุ่ม **Code** > **Codespaces** > **Create codespace on main**
   - รอ 1-2 นาทีให้ Codespace เริ่มทำงาน

2. **ตั้งค่า Environment Variables**

```bash
# คัดลอกไฟล์ตัวอย่าง
cp .env.example .env

# แก้ไขไฟล์ .env
nano .env
```

ใส่ API Keys ของคุณ แล้วกด `Ctrl+O` > `Enter` > `Ctrl+X`

3. **รันโปรแกรม**

```bash
python main-cloud.py
```

4. **คัดลอก Webhook URL**  
   Codespaces จะแสดง Public URL อัตโนมัติ เช่น:
   ```
   https://your-codespace-8000.app.github.dev/webhook
   ```

5. **ตั้งค่า LINE Webhook** (ดูขั้นตอนด้านล่าง)

📖 **อ่านเพิ่มเติม**: [RUN_WITH_CODESPACES.md](../RUN_WITH_CODESPACES.md)

---

### วิธีที่ 2: รันบนเครื่องตัวเอง (Local)

#### 1. ติดตั้ง Dependencies

```bash
pip install -r requirements.txt
```

#### 2. ตั้งค่า Environment Variables

คัดลอกไฟล์ `.env.example` เป็น `.env`:

```bash
cp .env.example .env
```

แก้ไขไฟล์ `.env` และใส่ค่าต่อไปนี้:

```env
LINE_CHANNEL_ACCESS_TOKEN=your_line_channel_access_token_here
LINE_CHANNEL_SECRET=your_line_channel_secret_here
GEMINI_API_KEY=your_gemini_api_key_here
PORT=8000
```

#### วิธีการหา API Keys:

**LINE Messaging API:**
1. ไปที่ [LINE Developers Console](https://developers.line.biz/console/)
2. สร้าง Provider และ Channel ใหม่ (Messaging API)
3. คัดลอก `Channel Access Token` และ `Channel Secret`

**Google Gemini API:**
1. ไปที่ [Google AI Studio](https://makersuite.google.com/app/apikey)
2. สร้าง API Key ใหม่
3. คัดลอก API Key

#### 3. รันโปรแกรม

**ตัวเลือก 3.1: ใช้ ngrok (Public URL อัตโนมัติ)**

```bash
python main.py
```

Bot จะเปิด ngrok tunnel และแสดง Public URL ให้อัตโนมัติ

**ตัวเลือก 3.2: ใช้ Cloudflare Tunnel**

```bash
python main-cloud.py
# จากนั้นเปิด Terminal อื่น:
./start_cloudflare.sh
```

📖 **อ่านเพิ่มเติม**: 
- [RUN_WITH_PYNGROK.md](../RUN_WITH_PYNGROK.md)
- [RUN_WITH_CLOUDFLARE.md](../RUN_WITH_CLOUDFLARE.md)

---

### ตั้งค่า Webhook บน LINE (ทุกวิธี)

1. ไปที่ [LINE Developers Console](https://developers.line.biz/console/)
2. เลือก Channel ของคุณ
3. ไปที่ **Messaging API** > **Webhook settings**
4. ใส่ Webhook URL (ตามวิธีที่คุณเลือก):
   - Codespaces: `https://your-codespace-8000.app.github.dev/webhook`
   - ngrok: `https://xxxx.ngrok-free.app/webhook`
   - Cloudflare: `https://your-tunnel.trycloudflare.com/webhook`
5. กด **Update** และ **Verify** → ต้องได้ ✅ Success
6. เปิด **Use webhook** = ON
7. (แนะนำ) ปิด **Auto-reply messages**

## 📱 วิธีใช้งานบน LINE

1. **เพิ่มบอทเป็นเพื่อน**: สแกน QR Code จาก LINE Developers Console
2. **เริ่มต้น**: ส่งข้อความ `เช็คสิทธิ์เคลมด่วน`
3. **กรอกข้อมูล**: ส่งชื่อและทะเบียนรถในรูปแบบ: `นายสมชาย เข็มกลัด, 1กข1234`
4. **ส่งรูปภาพ**: ส่งรูปภาพความเสียหายของรถ
5. **รับผลวิเคราะห์**: บอทจะวิเคราะห์และแจ้งผลภายใน 10-30 วินาที

## 🧪 ข้อมูลทดสอบ (Mock Data)

บอทมีข้อมูลตัวอย่างสำหรับทดสอบ (อยู่ในไฟล์ `mock_data.py`):

### ตัวอย่างที่ 1 - ประกันชั้น 1
- **ชื่อ**: นายสมชาย เข็มกลัด
- **ทะเบียน**: 1กข1234
- **รุ่นรถ**: Toyota Camry 2.5 Hybrid (2023)
- **ประเภท**: ชั้น 1 (เคลมได้ทุกกรณี)
- **Excess**: 5,000 บาท

### ตัวอย่างที่ 2 - ประกันชั้น 2+
- **ชื่อ**: นางสมหญิง ใจดี
- **ทะเบียน**: 2กท5678
- **รุ่นรถ**: Honda Civic RS (2022)
- **ประเภท**: ชั้น 2+ (ต้องมีคู่กรณี)
- **Excess**: 3,000 บาท

### ตัวอย่างที่ 3 - ประกันชั้น 1 (SUV)
- **ชื่อ**: นายวิชัย รักษ์ดี
- **ทะเบียน**: 3กก9999
- **รุ่นรถ**: Mazda CX-5 2.5 Turbo (2024)
- **ประเภท**: ชั้น 1 (เคลมได้ทุกกรณี)
- **Excess**: 7,000 บาท

## 🔧 การปรับแต่ง

### เพิ่มข้อมูลกรมธรรม์

แก้ไขไฟล์ `mock_data.py` ใน Dictionary `MOCK_POLICIES`:

```python
MOCK_POLICIES = {
    "ชื่อผู้เอาประกัน_ทะเบียนรถ": {
        "policy_number": "POL-2024-XXXXXX",
        "name": "ชื่อ-นามสกุล",
        "plate": "ทะเบียนรถ",
        "car_model": "ยี่ห้อและรุ่นรถ",
        "car_year": "2024",
        "insurance_type": "ชั้น 1",
        "insurance_company": "ชื่อบริษัทประกัน",
        "policy_start": "01/01/2024",
        "policy_end": "31/12/2024",
        "coverage": {
            "own_damage": "คุ้มครองเต็ม (ไม่ต้องมีคู่กรณี)",
            "third_party": "ไม่จำกัดจำนวนเงิน",
            "theft": "คุ้มครอง",
            "fire": "คุ้มครอง"
        },
        "excess": 5000,
        "status": "active"
    }
}
```

### ปรับแต่ง AI Prompt

แก้ไขฟังก์ชัน `analyze_damage_with_gemini()` ในส่วน `system_prompt`

### เชื่อมต่อฐานข้อมูลจริง

แทนที่ Dictionary ใน `get_policy_info()` ด้วยการเชื่อมต่อกับ Database เช่น:
- PostgreSQL + SQLAlchemy
- MongoDB + Motor
- MySQL + aiomysql

## 📂 โครงสร้างโปรเจกต์

```
LINE_ASST/
├── main.py              # ไฟล์หลักของโปรแกรม (FastAPI + LINE Bot + Gemini AI)
├── mock_data.py         # ข้อมูล Mock สำหรับกรมธรรม์ (สำหรับทดสอบ)
├── requirements.txt     # Python dependencies
├── .env.example         # ตัวอย่างไฟล์ environment variables
├── .env                 # ไฟล์ค่า config จริง (ห้าม commit)
├── .gitignore           # ไฟล์ที่ไม่ต้อง commit
├── README.md            # เอกสารนี้
├── SETUP_GUIDE.md       # คู่มือติดตั้งแบบละเอียด
└── MOCK_DATA_GUIDE.md   # คู่มือการจัดการข้อมูล Mock
```

## 🔒 ความปลอดภัย

- ❌ **ห้าม commit ไฟล์ `.env`** ที่มี API Keys จริง
- ✅ ใช้ Environment Variables สำหรับข้อมูลสำคัญ
- ✅ Verify LINE Signature ในทุก webhook request
- ✅ ใช้ HTTPS สำหรับ production

## 🐛 การแก้ปัญหา

### บอทไม่ตอบกลับ
- ตรวจสอบว่า Webhook URL ถูกต้องและเข้าถึงได้
- ตรวจสอบ logs ใน terminal
- ตรวจสอบว่า Channel Access Token ถูกต้อง

### Gemini AI ไม่ทำงาน
- ตรวจสอบ API Key
- ตรวจสอบ quota การใช้งาน API
- ดู error logs ใน terminal

### รูปภาพไม่สามารถดาวน์โหลดได้
- ตรวจสอบว่า Channel Access Token ถูกต้อง
- ตรวจสอบ network connectivity

## 📝 License

โปรเจกต์นี้เป็นตัวอย่างสำหรับการศึกษา สามารถนำไปพัฒนาต่อได้อย่างอิสระ

## 👨‍💻 Developer

สร้างด้วย ❤️ โดยใช้ FastAPI + LINE Bot SDK + Google Gemini AI

## 📚 เอกสารเพิ่มเติม

### 🚀 คู่มือการ Deploy
- **[RUN_WITH_CODESPACES.md](../RUN_WITH_CODESPACES.md)**: ⭐ วิธีรันบน GitHub Codespaces (แนะนำ)
- **[RUN_WITH_CLOUDFLARE.md](../RUN_WITH_CLOUDFLARE.md)**: วิธีใช้ Cloudflare Tunnel (URL คงที่)
- **[RUN_WITH_PYNGROK.md](../RUN_WITH_PYNGROK.md)**: วิธีใช้ ngrok (ทดสอบเร็ว)
- **[RUN_LOCAL.md](RUN_LOCAL.md)**: วิธีรันแบบ Local พร้อม Tunnel

### 📖 คู่มือการตั้งค่า
- **[SETUP_GUIDE.md](SETUP_GUIDE.md)**: คู่มือติดตั้งทีละขั้นตอน, วิธีขอ API Keys, การแก้ปัญหา
- **[MOCK_DATA_GUIDE.md](MOCK_DATA_GUIDE.md)**: คู่มือการจัดการข้อมูล Mock และวิธีเปลี่ยนไปใช้ Database จริง
- **[POLICY_DOCUMENT_GUIDE.md](POLICY_DOCUMENT_GUIDE.md)**: คู่มือการจัดการเอกสารกรมธรรม์

## 🔗 ลิงก์ที่เป็นประโยชน์

- [LINE Messaging API Documentation](https://developers.line.biz/en/docs/messaging-api/)
- [LINE Bot SDK Python](https://github.com/line/line-bot-sdk-python)
- [Google Gemini API Documentation](https://ai.google.dev/docs)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)

---

**หมายเหตุ**: โปรเจกต์นี้ใช้ Mock Data สำหรับการสาธิต ในการใช้งานจริงควรเชื่อมต่อกับฐานข้อมูลที่เหมาะสม
