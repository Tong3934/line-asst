# 📄 คู่มือการใช้เอกสารกรมธรรม์กับ AI

## 🎯 ภาพรวม

ระบบนี้สามารถให้ AI อ่านเอกสารกรมธรรม์จริง (PDF/รูปภาพ) และวิเคราะห์ความคุ้มครองโดยตรง แทนการใช้ข้อมูลที่ hardcode ใน mock_data

## 📦 สิ่งที่ได้แก้ไขแล้ว

### 1. ✅ แก้ไข `mock_data.py`
เพิ่ม field `policy_document_base64` ในแต่ละ policy:

```python
"policy_document_base64": None  # ใส่ base64 string ของเอกสารกรมธรรม์ตรงนี้
```

### 2. ✅ แก้ไข `%%writefile main.py` 
- แก้ไขฟังก์ชัน `analyze_damage_with_gemini()` ให้รับและประมวลผลเอกสารกรมธรรม์
- เพิ่มการตรวจสอบว่ามีเอกสารหรือไม่
- ส่งทั้งรูปภาพความเสียหายและเอกสารกรมธรรม์ไปให้ AI วิเคราะห์

## 🔧 ขั้นตอนการใช้งาน

### ขั้นตอนที่ 1: แปลงเอกสาร PDF/รูปภาพเป็น Base64

#### วิธีที่ 1: ใช้ Python Script

```python
import base64

# สำหรับไฟล์ PDF
with open('policy_document.pdf', 'rb') as f:
    pdf_base64 = base64.b64encode(f.read()).decode('utf-8')
    print(pdf_base64)

# สำหรับรูปภาพ (JPG/PNG)
with open('policy_document.jpg', 'rb') as f:
    image_base64 = base64.b64encode(f.read()).decode('utf-8')
    print(image_base64)
```

#### วิธีที่ 2: ใช้ Command Line

```bash
# macOS/Linux
base64 -i policy_document.pdf -o policy_base64.txt

# หรือแบบเดียว
base64 policy_document.pdf | pbcopy  # คัดลอกไปยัง clipboard
```

#### วิธีที่ 3: ใช้ Online Tool
- ไปที่ https://base64.guru/converter/encode/pdf
- อัพโหลดไฟล์ PDF
- คัดลอก Base64 string

### ขั้นตอนที่ 2: เพิ่ม Base64 ลงใน mock_data.py

แก้ไขไฟล์ `mock_data.py`:

```python
MOCK_POLICIES = {
    "นายสมชาย เข็มกลัด_1กข1234": {
        "policy_number": "POL-2024-001234",
        "title_name": "นาย",
        "first_name": "สมชาย ",
        "last_name": "เข็มกลัด",
        "plate": "1กข1234",
        # ... ข้อมูลอื่นๆ ...
        
        # วาง Base64 string ที่ได้จากขั้นตอนที่ 1 ตรงนี้
        "policy_document_base64": "JVBERi0xLjQKJeLjz9MKMyAwIG9iago8PC9GaWx0ZXIvRmxhdGVEZWNvZGUvTGVu..."
    },
}
```

### ขั้นตอนที่ 3: ทดสอบระบบ

1. รัน LINE Bot
2. ส่งข้อความ `เช็คสิทธิ์เคลมด่วน`
3. กรอกชื่อและทะเบียนรถ
4. ส่งรูปภาพความเสียหาย
5. AI จะอ่านเอกสารกรมธรรม์และตอบตามข้อมูลจริง

## 🤖 การทำงานของ AI

เมื่อมีเอกสารกรมธรรม์ AI จะ:

1. **อ่านเอกสารกรมธรรม์** (รูปภาพที่สอง)
   - ประเภทประกันจริง
   - ความคุ้มครอง Own Damage
   - เงื่อนไขการเคลม
   - ค่า Excess จริง
   
2. **วิเคราะห์รูปภาพความเสียหาย** (รูปภาพแรก)
   - ประเภทความเสียหาย
   - ขอบเขตความเสียหาย
   
3. **เปรียบเทียบและตัดสิน**
   - ตรวจสอบว่าอยู่ในความคุ้มครองหรือไม่
   - คำนวณค่าใช้จ่ายตามข้อมูลจริง
   - ให้คำแนะนำที่ถูกต้อง

## 📊 ตัวอย่าง Output

**กรณีมีเอกสารกรมธรรม์:**

```
📄 **ข้อมูลจากเอกสารกรมธรรม์**
- ประเภทประกัน: ชั้น 1 (อ่านจากเอกสาร)
- ความคุ้มครอง Own Damage: คุ้มครองเต็ม ไม่ต้องมีคู่กรณี
- ค่าเสียหายส่วนแรก (Excess): 5,000 บาท
- เงื่อนไขการเคลม: สามารถเคลมได้ทุกกรณี

🔍 **ผลการวิเคราะห์ความเสียหาย**
จากรูปภาพพบว่ารถมีความเสียหายที่กันชนหน้า...
```

**กรณีไม่มีเอกสาร (ใช้ mock_data):**

```
ใช้ข้อมูลจากระบบ (ไม่ใช่จากเอกสารจริง)
...
```

## ⚠️ ข้อควรระวัง

1. **ขนาดไฟล์**: 
   - Base64 string จะใหญ่กว่าไฟล์ต้นฉบับประมาณ 33%
   - แนะนำให้ใช้รูปภาพ (JPG) แทน PDF ถ้าทำได้
   - ขนาดไม่ควรเกิน 4 MB

2. **ความละเอียด**:
   - รูปภาพต้องชัดพอที่ AI จะอ่านข้อความได้
   - แนะนำ DPI ≥ 150

3. **รูปแบบไฟล์**:
   - รองรับ: PDF, JPG, PNG, WEBP
   - แนะนำ: JPG (ขนาดเล็กกว่า)

4. **ต้นทุน API**:
   - การส่งเอกสารเพิ่มเติมจะใช้ token มากขึ้น
   - ประมาณ 2-3 เท่าของการส่งแค่รูปภาพเดียว

## 💡 Best Practices

### สำหรับ Production

1. **เก็บในฐานข้อมูล**:
   ```python
   # แทนที่จะเก็บใน Python dict
   # ควรเก็บใน Database
   CREATE TABLE policies (
       policy_number VARCHAR PRIMARY KEY,
       ...
       policy_document_base64 TEXT,
       document_type VARCHAR(10)  -- 'pdf', 'jpg', 'png'
   );
   ```

2. **ใช้ Cloud Storage**:
   ```python
   # เก็บไฟล์จริงใน S3/Google Cloud Storage
   # เก็บแค่ URL ใน Database
   "policy_document_url": "https://s3.../policy_POL-2024-001234.pdf"
   ```

3. **Cache การประมวลผล**:
   ```python
   # Cache ผลลัพธ์จาก AI เพื่อประหยัดต้นทุน
   # ถ้าเอกสารไม่เปลี่ยน ใช้ผลเดิม
   ```

4. **OCR แยก**:
   ```python
   # ใช้ OCR แยกข้อความจากเอกสารก่อน
   # แล้วส่งแค่ข้อความไปให้ AI (ประหยัดกว่า)
   ```

## 🔗 Related Files

- `mock_data.py` - เพิ่ม field `policy_document_base64`
- `%%writefile main.py` - แก้ไขฟังก์ชัน `analyze_damage_with_gemini()`
- `README.md` - เอกสารหลัก

## 🆘 Troubleshooting

### ปัญหา: AI อ่านเอกสารไม่ได้

**สาเหตุ**:
- เอกสารไม่ชัด
- ขนาดไฟล์ใหญ่เกินไป
- รูปแบบไฟล์ไม่รองรับ

**วิธีแก้**:
1. แปลง PDF เป็นรูปภาพ (JPG) ก่อน:
   ```python
   from pdf2image import convert_from_path
   
   images = convert_from_path('policy.pdf', dpi=200)
   images[0].save('policy.jpg', 'JPEG', quality=85)
   ```

2. บีบอัดรูปภาพ:
   ```python
   from PIL import Image
   
   img = Image.open('policy.jpg')
   img = img.resize((1200, 1600))  # ลดขนาด
   img.save('policy_compressed.jpg', quality=85)
   ```

### ปัญหา: Error "Payload too large"

**วิธีแก้**:
- ลดขนาดรูปภาพ
- แปลงเป็น JPG quality 70-85
- แยกส่งทีละหน้า (ถ้าเป็น PDF หลายหน้า)

---

**หมายเหตุ**: คู่มือนี้อธิบายวิธีการใช้ฟีเจอร์ที่แก้ไขไว้แล้ว ไม่ต้องทำอะไรเพิ่มเติมนอกจากเพิ่ม Base64 ลงใน mock_data.py
