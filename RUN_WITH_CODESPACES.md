# 🚀 วิธีรัน LINE Bot บน GitHub Codespaces

GitHub Codespaces เป็นตัวเลือกที่ง่ายและสะดวกที่สุดสำหรับการรัน LINE Bot เพราะไม่ต้องติดตั้ง tunnel service เพิ่มเติม (ไม่ต้องใช้ ngrok หรือ Cloudflare)

## 📋 ข้อดีของการใช้ GitHub Codespaces

✅ **ไม่ต้องติดตั้งอะไรเพิ่ม** - Built-in port forwarding  
✅ **Public URL อัตโนมัติ** - ได้ HTTPS URL ทันที  
✅ **ฟรี 60 ชั่วโมง/เดือน** - สำหรับบัญชีฟรี  
✅ **เข้าถึงได้จากทุกที่** - ใช้งานผ่าน Browser  
✅ **พร้อม VS Code** - แก้ไขโค้ดได้ทันที

---

## 🛠️ ขั้นตอนการติดตั้งและรัน

### 1️⃣ สร้าง Codespace

1. ไปที่ GitHub Repository ของคุณ
2. คลิกปุ่ม **Code** > **Codespaces** > **Create codespace on main**
3. รอให้ Codespace เปิดขึ้นมา (ประมาณ 1-2 นาที)

### 2️⃣ ติดตั้ง Dependencies

เปิด Terminal ใน Codespaces และรันคำสั่ง:

```bash
pip install -r requirements.txt
```

### 3️⃣ ตั้งค่า Environment Variables

สร้างไฟล์ `.env` ในโฟลเดอร์หลัก:

```bash
nano .env
```

แล้วใส่ข้อมูลต่อไปนี้:

```env
LINE_CHANNEL_ACCESS_TOKEN=your_line_channel_access_token_here
LINE_CHANNEL_SECRET=your_line_channel_secret_here
GEMINI_API_KEY=your_gemini_api_key_here
PORT=8000
```

บันทึกไฟล์: กด `Ctrl + O` แล้ว `Enter` จากนั้นกด `Ctrl + X`

### 4️⃣ รัน Bot

```bash
python main-cloud.py
```

### 5️⃣ ดู Webhook URL

เมื่อ Server เริ่มทำงาน คุณจะเห็นข้อความแบบนี้:

```
🎉 GitHub Codespaces - LINE Insurance Claim Bot
======================================================================
📍 Local Server: http://localhost:8000
🌐 Public URL: https://your-codespace-name-8000.app.github.dev
🔗 Webhook URL: https://your-codespace-name-8000.app.github.dev/webhook
❤️  Health Check: https://your-codespace-name-8000.app.github.dev/health
======================================================================
```

**คัดลอก Webhook URL** ไว้ (จะนำไปใส่ใน LINE Developers Console)

### 6️⃣ ตรวจสอบ Port Visibility

1. ใน Codespaces ไปที่แท็บ **PORTS** (ด้านล่างของหน้าจอ)
2. ค้นหา Port `8000`
3. ตรวจสอบว่า **Visibility = Public** 🌐
4. ถ้าเป็น **Private** 🔒 ให้:
   - คลิกขวาที่ Port `8000`
   - เลือก **Port Visibility** > **Public**

> ⚠️ **สำคัญ!** ถ้า Port เป็น Private, LINE จะไม่สามารถเรียก Webhook ได้

---

## 🔗 ตั้งค่า LINE Webhook

### ขั้นตอน:

1. ไปที่ [LINE Developers Console](https://developers.line.biz/console/)
2. เลือก **Channel** ของคุณ
3. ไปที่ **Messaging API** > **Webhook settings**
4. วาง **Webhook URL** ที่ได้จาก Codespaces:
   ```
   https://your-codespace-name-8000.app.github.dev/webhook
   ```
5. กดปุ่ม **Update**
6. กดปุ่ม **Verify** → ต้องได้ ✅ **Success**
7. เปิด **Use webhook** = ON

---

## ✅ ทดสอบการทำงาน

### 1. เช็ค Health Check

เปิด Browser แล้วไปที่:

```
https://your-codespace-name-8000.app.github.dev/health
```

ควรได้ผลลัพธ์:

```json
{
  "status": "healthy",
  "line_configured": true,
  "gemini_configured": true
}
```

### 2. ทดสอบกับ LINE Bot

1. เปิด LINE App
2. เพิ่ม Bot เป็นเพื่อน
3. ส่งข้อความ: **"เช็คสิทธิ์เคลมด่วน"**
4. Bot ควรตอบกลับทันที

---

## 🔄 การอัพเดทโค้ด

เมื่อแก้ไขโค้ดใน Codespaces:

1. บันทึกไฟล์ที่แก้ไข
2. กด `Ctrl + C` ใน Terminal เพื่อหยุด Server
3. รันใหม่: `python main-cloud.py`

---

## 💡 Tips & Troubleshooting

### ⚠️ ปัญหา: Webhook Verify ไม่ผ่าน

**แก้ไข:**

- ตรวจสอบว่า Port Visibility = **Public**
- ตรวจสอบว่า Server ยังทำงานอยู่
- ลอง Restart Server: `Ctrl + C` แล้วรันใหม่
- ตรวจสอบ Firewall ใน Codespaces Settings

### ⚠️ ปัญหา: URL เปลี่ยนทุกครั้งที่สร้าง Codespace ใหม่

**คำอธิบาย:**  
Codespace แต่ละตัวจะได้ URL ที่ต่างกัน ดังนั้นถ้าคุณลบ Codespace และสร้างใหม่ ต้องอัพเดท Webhook URL ใน LINE Console ใหม่ทุกครั้ง

**แก้ไข:**

- อย่าลบ Codespace ถ้ายังใช้งานอยู่ (สามารถ Stop และ Start ใหม่ได้)
- หรือใช้ [Cloudflare Tunnel](./RUN_WITH_CLOUDFLARE.md) เพื่อให้ได้ URL ที่คงที่

### ⚠️ ปัญหา: Codespace หมดเวลาใช้งาน (Timeout)

**คำอธิบาย:**  
Codespace จะหยุดทำงานอัตโนมัติเมื่อไม่มีการใช้งานเกิน 30 นาที (ค่าเริ่มต้น)

**แก้ไข:**

- เปิด Codespace ใหม่จาก GitHub (ข้อมูลยังอยู่)
- รัน Server ใหม่: `python main-cloud.py`
- เปลี่ยนค่า timeout: **Settings** > **Codespaces** > **Default idle timeout**

### 💰 ค่าใช้จ่าย

- **GitHub Free**: 60 ชั่วโมง/เดือน (ประมาณ 2 ชั่วโมง/วัน)
- **GitHub Pro**: 90 ชั่วโมง/เดือน
- ถ้าเกิน: จะเสียค่าใช้จ่ายตามการใช้งาน

**แนะนำ:** ปิด Codespace เมื่อไม่ใช้งาน (จะไม่เสีย quota)

---

## 🆚 เปรียบเทียบกับวิธีอื่น

| วิธี                | ความง่าย | ฟรี | URL คงที่ | เหมาะกับ                |
| ------------------- | ------- | --- | --------- | ----------------------- |
| **GitHub Codespaces** | ⭐⭐⭐⭐⭐   | ✅  | ❌        | ทดสอบ/พัฒนา             |
| **Cloudflare Tunnel** | ⭐⭐⭐⭐    | ✅  | ✅        | Production              |
| **ngrok**           | ⭐⭐⭐     | ✅  | ❌        | ทดสอบชั่วคราว           |
| **Railway/Render**  | ⭐⭐⭐     | ✅  | ✅        | Production (Deploy ถาวร) |

---

## 🎯 สรุป

GitHub Codespaces เหมาะสำหรับ:

- 🧪 **ทดสอบและพัฒนา Bot**
- 🚀 **เริ่มต้นใช้งานได้ทันที** (ไม่ต้องติดตั้งอะไร)
- 🎓 **เรียนรู้และทดลอง LINE Bot API**

สำหรับการใช้งานจริง (Production) แนะนำ:

- [Cloudflare Tunnel](./RUN_WITH_CLOUDFLARE.md) - ถ้าต้องการ URL คงที่และฟรี
- [Deploy บน Railway/Render](./Readme/SETUP_GUIDE.md) - ถ้าต้องการ uptime 24/7

---

## 📚 เอกสารที่เกี่ยวข้อง

- [GitHub Codespaces Documentation](https://docs.github.com/en/codespaces)
- [วิธีใช้ Cloudflare Tunnel](./RUN_WITH_CLOUDFLARE.md)
- [วิธีใช้ ngrok](./RUN_WITH_PYNGROK.md)
- [คู่มือการติดตั้งทั่วไป](./Readme/SETUP_GUIDE.md)

---

**Happy Coding! 🎉**
