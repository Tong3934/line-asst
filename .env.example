##############################################################################
# .env.example — Copy to .env and fill in real values.
# NEVER commit .env to version control.
#
# 12-Factor III: All config stored in environment, not code.
##############################################################################

# ── LINE Messaging API (required) ─────────────────────────────────────────────
LINE_CHANNEL_ACCESS_TOKEN=your_line_channel_access_token_here
LINE_CHANNEL_SECRET=your_line_channel_secret_here

# ── Google Gemini AI (required) ───────────────────────────────────────────────
GEMINI_API_KEY=your_gemini_api_key_here

# ── ngrok (required for Docker tunnel) ───────────────────────────────────────
NGROK_AUTHTOKEN=your_ngrok_auth_token_here

# ── Application Settings (optional — defaults shown) ─────────────────────────
PORT=8000
DATA_DIR=/data

# ── AI Model / Pricing (optional overrides) ───────────────────────────────────
GEMINI_MODEL=models/gemini-2.5-flash
GEMINI_PRICE_INPUT=0.00035
GEMINI_PRICE_OUTPUT=0.00105

# ── LINE API Hosts (override for mock/local testing) ──────────────────────────
LINE_API_HOST=https://api.line.me
LINE_DATA_API_HOST=https://api-data.line.me

# ── Dev / mock-chat only ──────────────────────────────────────────────────────
BOT_URL=http://localhost:8000

# ── Auto-pull from git repo on startup (optional) ─────────────────────────────
# REPO_URL=https://github.com/your-org/line-asst.git
# BRANCH=main
